describe("Testing general functionnality and error handling of the libwidget module. v0.2.0", function () {
    //var Alloy = {
        //createController: jasmine.createSpy('createControllerSpy'),
        //createWidget: jasmine.createSpy('createWidgetSpy')
    //};

    var Alloy = require('alloy'); 
    
    it("Correctly override Alloy createWidget and createController method", function () {
        expect(true).toBe(true);
    });
});
